[project]
name = "livecase-cora"
version = "1.0.0"
description = "Pipeline para diagnóstico, normalização e processamento de leads."
requires-python = ">=3.11"

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pyright]
typeCheckingMode = "basic"